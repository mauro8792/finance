NEXT_PUBLIC_API_URL=http://localhost:3001
# Producción con dominio propio (Go-live B):
# NEXT_PUBLIC_API_URL=https://api.example.com
#
# Go-live A gratis (Vercel rewrite → Render): same-origin del front.
# El browser llama /api/... en este host; vercel.json reescribe a Render.
# NEXT_PUBLIC_API_URL=https://finance-web-eta-ruby.vercel.app
# En Render: WEB_ORIGIN debe ser exactamente ese mismo origen (sin / final).
